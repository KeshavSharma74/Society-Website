import React from 'react';

const Hero = () => {
  return (
    // Add Tailwind classes here
    <div className="h-screen bg-[url('/hero.png')] bg-cover bg-center">
      {/* You can add content here later, like a title or call to action */}
      <div className="text-white"> {/* Example: Adding white text on top */}
        
      </div>
    </div>
  );
};

export default Hero;